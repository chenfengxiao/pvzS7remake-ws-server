S丐版 1.7.8 — 双人 Versus + 2服入口/隧道诊断同步版（2026-08-27）

主线同步：
- 1.7.6：保护伞啃咬同帧立即右弹 1.5 格，并锁定攻击 CD 到击退结束。
- 1.7.7：地刺穿刺补 bobsledSled；普通雪橇散架下车，变种雪橇/冰车为 100 伤害 + 0.625 格击退。
- 1.7.8：僵尸元素层数文字统一受 B 键“文字显示”开关控制。

Versus / 2服修复：
- 双人对战选服后的创建/加入页面改为静态 DOM，不再运行时 innerHTML 后直接 .onclick 绑定。
- 选中1/2服只做页面导航；真正网络连接在创建/加入房间时发生，消除 UI 初始化与网络握手竞态。
- 2服新增可观测诊断：区分 MQTT Broker 已连、家庭服 retained 在线、以及收到 welcome 的端到端隧道真正打通。
- 仍保留旧 IPv6 直连作为隐藏 fallback，但默认 2服为 MQTT/WSS 反向隧道。
