# 盲盒模仿者头显示修复

日期：2026-08-01

## 修改范围

- `src/js/12_animation_zombies.js`：从 `blind` 的植物头僵尸动画配置中移除 `cone:true`，因此生成的行走与攻击 Clip 不再包含 `armor` 路障层。
- `src/js/21_entity_registry.js`：将盲盒的精灵加载失败回退符号由 `🎁🚧` 改为 `🎁`。

## 明确保留

- `makeBlind()` 仍创建名为“盲盒路障”的 270 点一类防具。
- 伤害仍先结算该防具，防具被打掉后仍按原规则开盒。
- 指令盲盒、魅惑盲盒、递归盲盒与经验规则不变。

本次是纯显示修复，不改变盲盒数值和战斗语义。
